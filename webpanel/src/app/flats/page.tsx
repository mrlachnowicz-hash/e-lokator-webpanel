"use client";
import { useEffect, useMemo, useState } from "react";
import { collection, doc, getDoc, onSnapshot, query, where } from "firebase/firestore";
import { RequireAuth } from "../../components/RequireAuth";
import { Nav } from "../../components/Nav";
import { useAuth } from "../../lib/authContext";
import { db } from "../../lib/firebase";
import { buildFlatLabel } from "../../lib/flatMapping";
import { displayStreetName } from "../../lib/streetUtils";
import { getFlatApartmentNo as getDisplayApartmentNo, getFlatDisplayLabel, getFlatEmail, getFlatPhone, getFlatResidentName } from "../../lib/flatDisplay";

type Flat = any; type FormState = { id:string|null; street:string; buildingNo:string; apartmentNo:string; name:string; surname:string; email:string; phone:string; };
const emptyForm:FormState={id:null,street:"",buildingNo:"",apartmentNo:"",name:"",surname:"",email:"",phone:""};
const getResidentName=(flat:Flat)=>String(getFlatResidentName(flat));
const getApartmentNo=(flat:Flat)=>String(getDisplayApartmentNo(flat));
const getFlatLabel=(flat:Flat, streetNames?:Map<string,string>)=>{ const fallback=String(getFlatDisplayLabel(flat)); if(fallback && fallback!==String(flat.id||"")) return fallback; return String(flat.flatLabel || buildFlatLabel(displayStreetName(flat.street || flat.streetName, flat.streetId, streetNames), flat.buildingNo, getApartmentNo(flat))); };
export default function FlatsPage(){ const { profile } = useAuth(); const communityId = profile?.communityId || ""; const role = String(profile?.role || ""); const canEdit = ["MASTER","ACCOUNTANT"].includes(role); const [items,setItems]=useState<Flat[]>([]); const [form,setForm]=useState<FormState>(emptyForm); const [msg,setMsg]=useState<string|null>(null); const [busy,setBusy]=useState(false); const [seatInfo,setSeatInfo]=useState<any>({limit:null,used:0,remaining:null});
useEffect(()=>{ if(!communityId) return; let flatsCache:Flat[]=[]; let usersByFlatId=new Map<string,any>(); let payersByFlatId=new Map<string,any>(); let streetNamesById=new Map<string,string>(); const merge=()=>setItems([...flatsCache.map((flat)=>{ const user=usersByFlatId.get(flat.id); const payer=payersByFlatId.get(flat.id); return { ...flat, street: displayStreetName(flat.street || flat.streetName || payer?.street, flat.streetId || user?.streetId || payer?.streetId, streetNamesById) || flat.street || payer?.street || "", buildingNo: flat.buildingNo || payer?.buildingNo || "", apartmentNo: getApartmentNo(flat) || payer?.apartmentNo || "", residentUid: flat.residentUid || user?.uid || user?.id || null, userId: flat.userId || user?.uid || user?.id || null, displayName: flat.displayName || user?.displayName || payer?.displayName || "", name: flat.name || flat.firstName || user?.firstName || payer?.name || "", surname: flat.surname || flat.lastName || user?.lastName || payer?.surname || "", email: flat.email || user?.email || user?.mail || user?.contactEmail || payer?.email || "", phone: flat.phone || user?.phone || user?.phoneNumber || user?.mobile || payer?.phone || "", residentName: flat.residentName || user?.displayName || payer?.displayName || `${user?.firstName || payer?.name || ""} ${user?.lastName || payer?.surname || ""}`.trim() }; })].sort((a,b)=>getFlatLabel(a,streetNamesById).localeCompare(getFlatLabel(b,streetNamesById),"pl",{numeric:true}))); const uf=onSnapshot(query(collection(db,"communities",communityId,"flats")), async (snap)=>{ flatsCache=snap.docs.map((d)=>({id:d.id,...(d.data() as any)})); try{ const cs=await getDoc(doc(db,"communities",communityId)); const c:any=cs.data()||{}; const limit=Number(c.seatsTotal || c.panelSeats || c.panelSeatsLimit || c.seats || c.seatsLimit || c.totalSeats || c.maxSeats || c.purchasedSeats || c.seatsPurchased || c.flatsLimit || c.localsLimit || c.localiLimit || c.unitsLimit || c.licenses || c.seatCount || 0) || null; const used = typeof c.seatsUsed === "number" ? Math.max(Math.floor(c.seatsUsed), snap.size) : snap.size; setSeatInfo({ limit, used, remaining: limit == null ? null : Math.max(0, limit - used) }); } catch { setSeatInfo({limit:null,used:snap.size,remaining:null}); } merge(); }); const us=onSnapshot(query(collection(db,"communities",communityId,"streets")),(snap)=>{ streetNamesById=new Map(snap.docs.map((d)=>[d.id,String((d.data() as any).name || d.id)])); merge(); }); const uu=onSnapshot(query(collection(db,"users"),where("communityId","==",communityId)),(snap)=>{ usersByFlatId=new Map(snap.docs.map((d)=>({id:d.id,...(d.data() as any)})).filter((u:any)=>String(u.flatId||"").trim()).map((u:any)=>[String(u.flatId),u])); merge(); }); const up=onSnapshot(query(collection(db,"communities",communityId,"payers")),(snap)=>{ payersByFlatId=new Map(snap.docs.map((d)=>[d.id,{id:d.id,...(d.data() as any)}])); merge(); }); return ()=>{uf();us();uu();up();}; },[communityId]);
const stats=useMemo(()=>({ total:items.length, withResidents:items.filter((x)=>!!getResidentName(x)||!!x.residentUid||!!x.userId).length, withEmail:items.filter((x)=>!!String(x.email||"").trim()).length }),[items]);
const startEdit=(flat:Flat)=>setForm({ id:flat.id, street:String(flat.street||""), buildingNo:String(flat.buildingNo||""), apartmentNo:getApartmentNo(flat), name:String(flat.name||flat.firstName||""), surname:String(flat.surname||flat.lastName||""), email:String(flat.email||""), phone:String(flat.phone||"") });
async function saveFlat(){ if(!communityId||!form.street.trim()||!form.buildingNo.trim()||!form.apartmentNo.trim()) return; setBusy(true); try{ const token=await (await import("firebase/auth")).getAuth().currentUser?.getIdToken(); if(!token) throw new Error("Brak aktywnej sesji Firebase."); const response=await fetch("/api/upsert-flat",{ method:"POST", headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`}, body:JSON.stringify({communityId,...form})}); const data=await response.json().catch(()=>({} as any)); if(!response.ok) throw new Error(data?.error || "Błąd zapisu lokalu."); setForm(emptyForm); setMsg(data?.message || "Zapisano lokal."); } catch(e:any){ setMsg(e?.message || "Błąd zapisu lokalu."); } finally { setBusy(false);} }
async function removeFlat(flat:Flat){ if(!communityId || !window.confirm(`Usunąć lokal ${getFlatLabel(flat)}?`)) return; setBusy(true); try{ const token=await (await import("firebase/auth")).getAuth().currentUser?.getIdToken(); if(!token) throw new Error("Brak aktywnej sesji Firebase."); const response=await fetch("/api/remove-flat",{ method:"POST", headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`}, body:JSON.stringify({communityId,flatId:flat.id})}); const data=await response.json().catch(()=>({} as any)); if(!response.ok) throw new Error(data?.error || "Błąd usuwania lokalu."); setMsg(`Usunięto lokal: ${getFlatLabel(flat)}`); } finally { setBusy(false);} }
return <RequireAuth roles={["MASTER","ACCOUNTANT"]}><Nav /><div style={{padding:24,display:"grid",gap:16}}><h2>Lokale</h2><div style={{display:"grid",gap:12,maxWidth:980}}><div style={{border:"1px solid #e5e5e5",borderRadius:12,padding:16}}><div style={{display:"flex",gap:18,flexWrap:"wrap"}}><div><b>Łącznie lokali:</b> {stats.total}</div><div><b>Z przypisanym mieszkańcem:</b> {stats.withResidents}</div><div><b>Z adresem email:</b> {stats.withEmail}</div><div><b>Seats:</b> {seatInfo.limit == null ? `${seatInfo.used} / brak limitu` : `${seatInfo.used} / ${seatInfo.limit}`}</div><div><b>Pozostało:</b> {seatInfo.remaining == null ? '—' : Math.max(0, seatInfo.remaining)}</div></div></div>{canEdit?<div style={{border:"1px solid #e5e5e5",borderRadius:12,padding:16}}><h3>{form.id ? "Edytuj lokal" : "Dodaj lub zaktualizuj lokal"}</h3><div style={{display:"grid",gap:10,gridTemplateColumns:"1fr 1fr 1fr"}}><input placeholder="Ulica" value={form.street} onChange={(e)=>setForm({...form,street:e.target.value})} /><input placeholder="Nr budynku" value={form.buildingNo} onChange={(e)=>setForm({...form,buildingNo:e.target.value})} /><input placeholder="Nr lokalu" value={form.apartmentNo} onChange={(e)=>setForm({...form,apartmentNo:e.target.value})} /><input placeholder="Imię" value={form.name} onChange={(e)=>setForm({...form,name:e.target.value})} /><input placeholder="Nazwisko" value={form.surname} onChange={(e)=>setForm({...form,surname:e.target.value})} /><input placeholder="Email" value={form.email} onChange={(e)=>setForm({...form,email:e.target.value})} /><input placeholder="Telefon" value={form.phone} onChange={(e)=>setForm({...form,phone:e.target.value})} /></div><div style={{display:"flex",gap:10,marginTop:10}}><button onClick={saveFlat} disabled={busy}>Zapisz</button>{form.id?<button onClick={()=>setForm(emptyForm)} disabled={busy}>Anuluj edycję</button>:null}</div>{msg?<div style={{marginTop:10}}>{msg}</div>:null}</div>:null}</div><div style={{border:"1px solid #e5e5e5",borderRadius:12,overflow:"hidden",maxWidth:1280}}><div style={{display:"grid",gridTemplateColumns:"1.4fr .8fr 1.2fr 1.1fr .9fr 1fr",gap:12,padding:14,fontWeight:700,borderBottom:"1px solid rgba(229,229,229,0.25)"}}><div>Adres</div><div>Numer</div><div>Mieszkaniec</div><div>Email</div><div>Telefon</div><div>Akcje</div></div>{items.map((flat)=><div key={flat.id} style={{display:"grid",gridTemplateColumns:"1.4fr .8fr 1.2fr 1.1fr .9fr 1fr",gap:12,padding:14,borderTop:"1px solid rgba(229,229,229,0.12)",alignItems:"center"}}><div><div style={{fontWeight:700}}>{getFlatLabel(flat)}</div><div style={{opacity:.7,fontSize:13}}>Ulica: {flat.street || '—'} · Budynek: {flat.buildingNo || '—'}</div></div><div>{getApartmentNo(flat) || "—"}</div><div><div>{getResidentName(flat) || "Brak danych"}</div><div style={{opacity:.7,fontSize:13}}>{flat.residentUid || flat.userId ? "Powiązany z aplikacją" : "Brak użytkownika aplikacji"}</div></div><div>{getFlatEmail(flat) || "—"}</div><div>{getFlatPhone(flat) || "—"}</div><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><button onClick={()=>startEdit(flat)} disabled={!canEdit || busy}>Edytuj</button><button onClick={()=>removeFlat(flat)} disabled={!canEdit || busy}>Usuń</button></div></div>)}</div></div></RequireAuth>; }
